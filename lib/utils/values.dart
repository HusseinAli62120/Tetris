enum PieceType { L, J, I, O, S, T, Z }

enum Direction { left, right, down, clockwise, counterClockwise }

// How many cells in a row. NOT how many rows
const int rowLength = 10;
// How many cells in a column. NOT how many columns
const int colLength = 14;
